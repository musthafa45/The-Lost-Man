<<Version>>
1.0

<<Version changes>>
1.0
ÅERelease

1.1
ÅEFix : Color is fixed "Color.black"

<<How to use>>
1. Get instance "FadeController.Instance". You don't need attach to any objects.
2. Standard "FadeIn, Out(FadeTime)". Ex) FadeController.Instance.FadeIn(1.0f);
	2-2. FadeIn, Out(FadeTime, Color)
	2-1. FadeIn, Out(FadeTime, CallBack)
	2-2. FadeIn, Out(FadeTime, Color, CallBack)
3. You can get finished flag from Property "IsComplete" too.

<<Contact>>
Twitter:@isemito_niko